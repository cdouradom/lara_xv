# URL da Web App (Google Apps Script), terminando em /exec — não coloque segredos aqui.
# Copie para .env na raiz (o ficheiro .env não deve ir para o Git).
VITE_RSVP_SUBMIT_URL=https://script.google.com/macros/s/SEU_DEPLOY/exec
